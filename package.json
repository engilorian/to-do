{
  "name": "to-do",
  "version": "1.0.0",
  "description": "A simple CLI-based to-do list application built with TypeScript.",
  "main": "todo.js",
  "scripts": {
    "start": "node todo.js",
    "build": "tsc",
    "dev": "tsc && node todo.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["to-do", "cli", "typescript"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.6",
    "typescript": "^5.7.3"
  }
}
